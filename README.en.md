## Working Engineer Kitty

Updates happen only when both having time and feeling motivated are true:

```js
function checkAndUpdate(hasTime, isMotivated) {
    if (!hasTime || !isMotivated) {
        return;  // nop
    }

    update();
}
```


[日本語](README.md) | English
